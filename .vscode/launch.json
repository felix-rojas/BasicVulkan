{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "WindowsLaunch",
            "program": "${workspaceFolder}/main.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}